[package]
name = "appids"
version = "0.1.0"
edition = "2024"

[dependencies]
isideload = { path = "../../isideload", default-features = false, features = ["install", "fs-storage"] }
tokio = { version = "1.49.0", features = ["rt-multi-thread", "macros"] }
tracing = "0.1.44"
tracing-subscriber = "0.3.22"
serde_json = "1.0.149"
rootcause = "0.12.0"